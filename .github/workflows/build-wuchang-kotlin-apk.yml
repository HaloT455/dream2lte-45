name: Build Wuchang Kotlin APK

on:
  push:
    branches:
      - wuchang-live-wallpaper
    paths:
      - 'WuchangLiveWallpaperKotlin/**'
      - '.github/workflows/build-wuchang-kotlin-apk.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: WuchangLiveWallpaperKotlin

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Configure Android SDK
        shell: bash
        run: |
          SDKMANAGER=/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platform-tools" "platforms;android-35" "build-tools;35.0.0"
          echo "ANDROID_HOME=/usr/local/lib/android/sdk" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=/usr/local/lib/android/sdk" >> "$GITHUB_ENV"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build Kotlin debug APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk app/build/outputs/apk/debug/VoThuongLiveWallpaper-Kotlin-v2.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: VoThuongLiveWallpaper-Kotlin-APK
          path: WuchangLiveWallpaperKotlin/app/build/outputs/apk/debug/VoThuongLiveWallpaper-Kotlin-v2.apk
          if-no-files-found: error
